'use strict';
// Напишите условие if для проверки, что переменная age1 находится в диапазоне между 14 и 90 включительно.
let age1=prompt('What is your age1?', '');
if (age1>=14 && age1<=90) {
  alert ('Verification successful');
} else {
  alert ('Verification is failed');
} 